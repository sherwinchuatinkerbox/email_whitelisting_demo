source 'https://rubygems.org'

gem 'rails', '4.0.0'

gem 'sass-rails', '~> 4.0.0'

gem 'uglifier', '>= 1.3.0'

gem 'coffee-rails', '~> 4.0.0'

gem 'jquery-rails'

gem 'turbolinks'

gem 'dotenv-rails'

group :development, :test do
  gem 'sqlite3'
  gem 'letter_opener_web'
  gem 'quiet_assets'
end

group :production do
  gem 'pg'
  gem 'sendgrid-rails'
  gem 'rails_12factor'
  # NOTES: alternative: safety_mailer (more recent, you should use this instead)
  # NOTES: heroku has an add-on called Mailtrap by Railsware
  gem 'mail_gate'
end


